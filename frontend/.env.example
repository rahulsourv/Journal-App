# Base URL of the Express backend, INCLUDING the /api suffix.
#
# Local:      http://localhost:5000/api
# Production: https://<your-friends-service>.onrender.com/api
#
# On Vercel, set this in Project Settings > Environment Variables
# (Vite only exposes variables prefixed with VITE_).
VITE_API_URL=https://journal-app-cep8.onrender.com/
